# DPSK - Trading Bot Dépendances
# ================================
# Installation : pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────────────
ccxt>=4.0.0,<6.0.0           # Multi-exchange API (Binance Futures)
pandas>=2.0.0                 # Data manipulation
numpy>=1.24.0                 # Numerical computing
ta>=0.10.0                    # Technical Analysis indicators

# ── Async & HTTP ──────────────────────────────────────────────────────
aiohttp>=3.8.0                # Async HTTP client (Telegram API)

# ── Testing ───────────────────────────────────────────────────────────
pytest>=7.0.0
pytest-asyncio>=0.21.0

# ── Dashboard API ─────────────────────────────────────────────────────
fastapi>=0.100.0              # REST API framework
uvicorn>=0.20.0               # ASGI server

# ── Configuration ─────────────────────────────────────────────────────
PyYAML>=6.0                   # YAML config parser

# ── Dépendances transitives (installées automatiquement) ──────────────
# - typing_extensions (par fastapi)
# - certifi, charset-normalizer, idna, urllib3 (par requests/aiohttp)
# - python-dateutil, six, pytz (par pandas)
# - frozenlist, multidict, yarl, aiosignal, attrs (par aiohttp)
# - starlette, annotated-types (par fastapi)
# - click, h11 (par uvicorn)
# - pycares, aiodns (optionnel, résolution DNS async)