You are an expert Senior Software Engineer and Quantitative Analyst specializing in algorithmic trading systems.

Your task is to audit the project and verify that the following 7 critical fixes have been correctly implemented. For each fix, inspect the relevant file(s), locate the exact code, and return a structured verdict.

---

## FIXES TO VERIFY

### FIX 1 — NameError: MAX_RETRIES undefined
File: `module1_data_v3.py`
Check: The final error log after exhausting retries must use `params['max_retries']` (or equivalent local variable), NOT the undefined `MAX_RETRIES` constant.
Expected pattern: `params["max_retries"]` or `params['max_retries']`
Forbidden pattern: bare `MAX_RETRIES`

### FIX 2 — Look-ahead bias in simulate_trade
File: `module4_backtest.py`
Check: Inside `simulate_trade`, the intra-candle order of SL/TP checks must depend on candle direction (bullish vs bearish). A bullish candle should check TP before SL; a bearish candle should check SL before TP.
Expected pattern: logic branching on `row["close"] >= row["open"]` or equivalent before checking SL/TP levels.
Forbidden pattern: always checking SL before TP1 unconditionally within the same candle.

### FIX 3 — Daily filter missing from backtest_multi.py
File: `backtest_multi.py`
Check: `run_single_backtest` must fetch daily OHLCV data and compute `daily_trend` for each symbol when `daily_filter_enabled` is True in config. The `daily_trend` must be passed as argument to `generate_signal`.
Expected pattern: call to `fetch_daily_all_async` and `compute_daily_trend` inside `run_single_backtest`, and `generate_signal(..., daily_trend=...)`.
Forbidden pattern: `generate_signal(symbol, df_sub, config)` called without `daily_trend` argument.

### FIX 4 — Blocking synchronous requests in async context
File: `bot_listener.py`
Check: All HTTP calls to the Telegram API (both GET for polling and POST for sending messages) must use `aiohttp` (async), not `requests` (sync).
Expected pattern: `aiohttp.ClientSession` with `await session.get(...)` and `await session.post(...)`
Forbidden pattern: `requests.get(...)` or `requests.post(...)` anywhere in `bot_listener.py`

### FIX 5 — Config singleton: no hot reload + global in bot_telegram.py
Files: `config_loader.py`, `bot_telegram.py`
Check A (config_loader): A `reload_config()` function must exist that resets `_config` and reloads from disk.
Check B (bot_telegram): `get_config()` must be called INSIDE `run_scan()` (or equivalent async function), not at module level as a global variable assignment.
Expected pattern in bot_telegram.py: `config = get_config()` inside an async function body.
Forbidden pattern: `config = get_config()` at top-level module scope (outside any function).

### FIX 6 — O(n²) indicator recalculation in backtest_multi.py
File: `backtest_multi.py`
Check: Inside `run_single_backtest`, `compute_indicators` must be called ONCE per symbol (before the while loop), and the inner loop must slice the already-enriched DataFrame. It must NOT call `compute_indicators` inside the while loop on a growing slice.
Expected pattern: `enriched = compute_indicators(clean, config, ...)` before `while i < end_test`, then `df_sub = enriched.iloc[:i+1]` inside the loop.
Forbidden pattern: `compute_indicators(enriched.iloc[:i+1], ...)` or `compute_indicators(df.iloc[:i+1], ...)` inside the while loop.

### FIX 7 — Missing advanced risk metrics
File: `backtest_multi.py` or `module4_backtest.py`
Check: `compute_metrics` (or equivalent function) must compute at minimum: max drawdown, Sharpe ratio, and Calmar ratio, in addition to profit factor and winrate.
Expected pattern: variables or keys named `max_drawdown`, `sharpe`, `calmar` (or similar) present in the metrics output.
Forbidden pattern: metrics output containing only `winrate`, `profit_factor`, `pnl_total` with no drawdown/sharpe/calmar.

---

## OUTPUT FORMAT

Return a markdown report with this exact structure for each fix:

### FIX [N] — [Title]
- **Status**: ✅ IMPLEMENTED | ❌ NOT IMPLEMENTED | ⚠️ PARTIAL
- **File**: `filename.py`
- **Evidence**: paste the exact relevant code snippet (max 10 lines)
- **Comment**: one sentence explaining what is correct, missing, or incomplete

---

## FINAL SUMMARY

| Fix | Title | Status |
|-----|-------|--------|
| 1 | NameError MAX_RETRIES | ✅ / ❌ / ⚠️ |
| 2 | Look-ahead bias simulate_trade | ✅ / ❌ / ⚠️ |
| 3 | Daily filter in backtest_multi | ✅ / ❌ / ⚠️ |
| 4 | Blocking requests in bot_listener | ✅ / ❌ / ⚠️ |
| 5 | Config hot reload + global scope | ✅ / ❌ / ⚠️ |
| 6 | O(n²) recalculation | ✅ / ❌ / ⚠️ |
| 7 | Advanced risk metrics | ✅ / ❌ / ⚠️ |

**Score: X / 7 fixes verified**

Be strict. Do not mark a fix as ✅ unless the exact expected pattern is present. Partial or incorrect implementations must be flagged ⚠️ or ❌ with a clear explanation.
